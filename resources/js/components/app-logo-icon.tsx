import type { SVGAttributes } from 'react';
import { Braces } from '@/components/bootstrap-icons';

export default function AppLogoIcon(props: SVGAttributes<SVGSVGElement>) {
    return <Braces {...props} />;
}
